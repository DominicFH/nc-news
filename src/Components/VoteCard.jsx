import React from 'react';

const VoteCard = (props) => {
    console.log(props)
    return (
        <div>
            <p>VOTE CARD PLACEHOLDER TEXT</p>
        </div>
    );
};

export default VoteCard;